export type Jogos = {
    id: number,
    name: string,
    slug: string,
    image_background: string,
    games:[
        {
            id: number,
            name: string,
            slug: string,
        }
    ]
        
    
}